MVC Pattern Design

Dataflow
User (input) -> View (requests) -> Controller
Controller (responds) -> View (output) -> User

Controller (requests) -> CSV Model
CSV Model (responds) -> Controller

Controller (requests) -> AI Model
AI Model (responds) -> Controller

chats/ saves chats as seperate files

Chats are dynamically generated and can recover from its folder being absent or metadata undefined
While the model is remotely downloaded if missing, it requires metadata to determine the model to fetch